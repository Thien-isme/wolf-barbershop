export type SaveToCartResponse = {
    Status?: number;
    MessageShow?: string; // default: "Hệ thống có lỗi, Vui lòng thử lại trong giây lát!"
    MessageHide?: string; // default: "None"
    Data?: any;
};
